# Demo application for fashion cloud 

### Setup & Run the app

    Install node, npm, mongodb
    Run `npm install` in the project directory
    Run `npm run start` and point your browser to http://localhost:3000/api/v1/health
